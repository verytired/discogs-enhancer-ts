# CI Workflow Setup

## Status
- [ ] Create GitHub Action workflow for CI (`.github/workflows/ci.yml`)
- [ ] Configure workflow to run on PRs to `master` and `release/*`
- [ ] Add steps for:
    - [ ] Checkout code
    - [ ] Setup Node.js
    - [ ] Install dependencies
    - [ ] Run Linting (optional but recommended)
    - [ ] Run Tests (`npm test`)
    - [ ] Build Check (`npm run build`)
